<Application x:Class="SportsMax.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- ============ Paleta SportsMax ============ -->
            <SolidColorBrush x:Key="BgDark"     Color="#FF0B1320"/>
            <SolidColorBrush x:Key="BgPanel"    Color="#FF131C2E"/>
            <SolidColorBrush x:Key="BgCard"     Color="#FF1A2640"/>
            <SolidColorBrush x:Key="BgCardAlt"  Color="#FF22324F"/>
            <SolidColorBrush x:Key="BgPopup"    Color="#FF1A2640"/>
            <SolidColorBrush x:Key="BgHover"    Color="#FF2D4570"/>
            <SolidColorBrush x:Key="Accent"     Color="#FF1E88E5"/>
            <SolidColorBrush x:Key="AccentDark" Color="#FF1565C0"/>
            <SolidColorBrush x:Key="AccentSoft" Color="#FF64B5F6"/>
            <SolidColorBrush x:Key="LiveRed"    Color="#FFE53935"/>
            <SolidColorBrush x:Key="SoonOrange" Color="#FFFB8C00"/>
            <SolidColorBrush x:Key="OkGreen"    Color="#FF43A047"/>
            <SolidColorBrush x:Key="FgPrimary"  Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="FgSecondary" Color="#FFCFD8DC"/>
            <SolidColorBrush x:Key="FgMuted"    Color="#FF90A4AE"/>
            <SolidColorBrush x:Key="Divider"    Color="#FF2A3A55"/>

            <!-- ============ Botones ============ -->
            <Style x:Key="BtnBase" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource Accent}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Br" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Br" Property="Opacity" Value="0.88"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Br" Property="Opacity" Value="0.7"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Br" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="BtnGreen" BasedOn="{StaticResource BtnBase}" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource OkGreen}"/>
            </Style>
            <Style x:Key="BtnRed" BasedOn="{StaticResource BtnBase}" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource LiveRed}"/>
            </Style>
            <Style x:Key="BtnGhost" BasedOn="{StaticResource BtnBase}" TargetType="Button">
                <Setter Property="Background" Value="#FF22324F"/>
            </Style>

            <!-- ============ TextBox con placeholder ============ -->
            <Style x:Key="SearchBox" TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource BgCardAlt}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="CaretBrush" Value="White"/>
                <Setter Property="BorderBrush" Value="{StaticResource Divider}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <Grid>
                                    <TextBlock x:Name="Placeholder"
                                               Text="{TemplateBinding Tag}"
                                               Foreground="{StaticResource FgMuted}"
                                               VerticalAlignment="Center"
                                               Margin="{TemplateBinding Padding}"
                                               IsHitTestVisible="False"
                                               Visibility="Collapsed"/>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                                  Margin="{TemplateBinding Padding}"
                                                  Background="Transparent"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============ ComboBox oscuro (popup + items) ============ -->
            <Style x:Key="ComboItemDark" TargetType="ComboBoxItem">
                <Setter Property="Background" Value="{StaticResource BgPopup}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="8,5"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Br"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Br" Property="Background" Value="{StaticResource BgHover}"/>
                                </Trigger>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Br" Property="Background" Value="{StaticResource AccentDark}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ComboDark" TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource BgCardAlt}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="{StaticResource Divider}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="ItemContainerStyle" Value="{StaticResource ComboItemDark}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border x:Name="Br"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="22"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"
                                                      TextBlock.Foreground="White"/>
                                    <Path Grid.Column="1"
                                          Data="M 0 0 L 8 0 L 4 5 Z"
                                          Fill="White"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                    <ToggleButton Grid.ColumnSpan="2"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                                <Popup x:Name="PART_Popup"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       PopupAnimation="Slide">
                                    <Border Background="{StaticResource BgPopup}"
                                            BorderBrush="{StaticResource Divider}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="320">
                                        <ScrollViewer>
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Br" Property="BorderBrush" Value="{StaticResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============ Boton de canal (pill) ============ -->
            <Style x:Key="ChannelBtn" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BgCardAlt}"/>
                <Setter Property="Foreground" Value="{StaticResource FgSecondary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Padding" Value="14,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Br" Background="{TemplateBinding Background}"
                                    CornerRadius="15" Padding="{TemplateBinding Padding}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock x:Name="Dot" Text="●" FontSize="9"
                                               Foreground="{StaticResource FgMuted}"
                                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Br" Property="Background" Value="{StaticResource BgHover}"/>
                                </Trigger>
                                <!-- Activo: marcado con Tag=ACTIVE -->
                                <Trigger Property="Tag" Value="ACTIVE">
                                    <Setter TargetName="Br" Property="Background" Value="{StaticResource Accent}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter TargetName="Dot" Property="Foreground" Value="#FF80FF80"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============ ScrollBar oscura ============ -->
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="{StaticResource BgPanel}"/>
                <Setter Property="Foreground" Value="{StaticResource Divider}"/>
                <Setter Property="Width" Value="10"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
